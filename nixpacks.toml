[variables]
NIXPACKS_POETRY_VERSION = "1.8.3"

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install poetry==1.8.3",
  ". /opt/venv/bin/activate && poetry install --no-dev --no-interaction --no-ansi"
]
